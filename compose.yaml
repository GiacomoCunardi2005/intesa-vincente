services:
  intesa-vincente:
    build:
      context: .
      network: host
    ports:
      - "5522:5522"
    environment:
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-https://intesa.cunardi.com}
    volumes:
      - intesa-records:/data
    restart: unless-stopped

volumes:
  intesa-records:
